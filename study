실행 -> python3
종료 -> exit()

 python 코드는 shell과 file에 저장할 수 있다.
 작성된 코드를 vscode와 같은 python interpreter에 전달된다.

python을 실행하고 나면 나오는 >>> 이부분이 명령할 수 있는 부분이다.

예를들어 파이썬에서 출력을 할 때, print(1+1) 라는 코드를 작성하게 되는데 shell에서는 print를 생략할 수 있다.

출력 print()
길이 len()
명령어를 연달아 사용할때, ';' 사용

file로 저장할 때, 폴더이름의 끝에 .py를 붙여준다.
코드를 저장할 떄 유용하게 사용할 수 있을것 같다.

주석처리 command + / = 

python 실행 단축키 command + '

debugger (디버그) = 오류를 해결하는 것

pypi -> pandas 파이썬 라이브러리를 위해 설치한다.

1. vscode의 Source Control 클릭
2. 업로드할 파일 커밋 -> 로컬 저장소에 반영하기 위함이다.
3. 터미널에 github에서 복사했던 주소를 다음 명령을 통해 실행한다.
git remote add origin [주소]
## 메인 브랜치는 master 이다.
## 브랜치의 이름을 master에서 main으로 변경한다.
git branch -m master main
git branch -> 브랜치 이름이 main으로 바뀐걸 확인할 수 있다. *main
4. github 저장소에 있는 내용을 로컬 저장소에 반영해주기 위해 처음에 한번 해주는 것
## 경고문이 안보이게 하기 위함 git config pull.rebase false
5. 한번 실행 해주어야 이후 업로드가 가능하다.
git pull origin main --allow-unrelated-histories
6. github에 코드를 업로드한다.
git push -u origin main